name: 🔐 Security Pipeline - ML Vulnerability Detection

on:
  pull_request:
    branches: [test]  # Solo se activa en PR hacia test
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write
  issues: write
  statuses: write
  actions: write

env:
  TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
  TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}

jobs:
  security-analysis:
    name: 🤖 ML Security Analysis
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: '**/package-lock.json'
      
      - name: 📦 Install ML dependencies
        run: |
          cd ml-security/backend
          pip install -r requirements.txt
      
      - name: ✅ Verificar modelos ML
        run: |
          echo "🔍 Verificando modelos entrenados..."
          
          # Verificar que existan los modelos necesarios
          if [ ! -f ml-security/models/vulnerability_detector.pkl ]; then
            echo "❌ Error: Modelo de detección no encontrado"
            exit 1
          fi
          
          if [ ! -f ml-security/models/cwe_classifier.pkl ]; then
            echo "❌ Error: Modelo de clasificación CWE no encontrado"
            exit 1
          fi
          
          echo "✅ Todos los modelos están disponibles"
      
      - name: 🔍 Escanear vulnerabilidades con ML
        id: scan
        continue-on-error: true
        run: |
          echo "🔍 Escaneando código con modelos ML..."
          echo "📊 Modelo 1: Detector binario (79.01% accuracy, 90.12% recall)"
          echo "📊 Modelo 2: Clasificador CWE (86.94% accuracy)"
          echo ""
          
          # Instalar dependencias para el script
          pip install requests numpy==2.0.0 scikit-learn==1.7.2
          
          # Verificar que existan los modelos
          echo "🔍 Verificando modelos ML..."
          ls -lh ml-security/models/*.pkl || { 
            echo "❌ No se encontraron modelos ML necesarios"
            echo "scan_status=failed" >> $GITHUB_OUTPUT
            echo "vulnerable=unknown" >> $GITHUB_OUTPUT
            exit 1
          }
          
          # Hacer ejecutable el script
          chmod +x .github/scripts/scan_vulnerabilities.py
          
          # Escanear directorios específicos
          echo "🔍 Escaneando cliente y servidor..."
          
          # Intentar escaneo con el script de Python
          python .github/scripts/scan_vulnerabilities.py cliente 2>&1 | tee scan_output.txt || true
          python .github/scripts/scan_vulnerabilities.py servidor 2>&1 | tee -a scan_output.txt || true
          
          # Verificar que se haya generado el reporte
          if [ -f "vulnerability_report.json" ]; then
            echo "✅ Reporte generado exitosamente"
            
            # Mostrar contenido del reporte
            echo "📄 Contenido del reporte:"
            cat vulnerability_report.json
            
            # Verificar vulnerabilidades
            if grep -q '"is_safe": false' vulnerability_report.json; then
              echo "vulnerable=true" >> $GITHUB_OUTPUT
              echo "scan_status=completed" >> $GITHUB_OUTPUT
              echo "❌ Se detectaron vulnerabilidades"
              exit 1
            else
              echo "vulnerable=false" >> $GITHUB_OUTPUT
              echo "scan_status=completed" >> $GITHUB_OUTPUT
              echo "✅ Código seguro - Sin vulnerabilidades detectadas"
            fi
          else
            echo "scan_status=failed" >> $GITHUB_OUTPUT
            echo "vulnerable=unknown" >> $GITHUB_OUTPUT
            echo "❌ Error: No se pudo generar el reporte de seguridad"
            cat scan_output.txt || true
            exit 1
          fi
      
      - name: 📤 Upload security report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: security-report
          path: security_report.json
          retention-days: 30
      
      - name: 📱 Enviar notificación a Telegram
        if: always()
        run: |
          pip install requests
          python .github/scripts/notify_telegram.py || echo "⚠️  Error enviando notificación"
      
      - name: 📊 Comment PR with results
        if: always() && steps.scan.outputs.scan_status == 'completed'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            
            // Intentar leer el reporte
            let report;
            try {
              if (fs.existsSync('vulnerability_report.json')) {
                report = JSON.parse(fs.readFileSync('vulnerability_report.json', 'utf8'));
              } else {
                throw new Error('vulnerability_report.json no encontrado');
              }
            } catch (error) {
              // Si no hay reporte, crear comentario de error
              const errorBody = `## ❌ Error en Análisis de Seguridad\n\n` +
                `No se pudo completar el análisis ML.\n\n` +
                `**Error:** ${error.message}\n\n` +
                `Por favor, revisa los logs del workflow para más detalles.`;
              
              await github.rest.issues.createComment({
                issue_number: context.issue.number,
                owner: context.repo.owner,
                repo: context.repo.repo,
                body: errorBody
              });
              return;
            }
            
            const status = report.is_safe ? '✅ APROBADO' : '❌ RECHAZADO';
            const emoji = report.is_safe ? '🟢' : '🔴';
            
            let body = `## ${emoji} Análisis de Seguridad ML - ${status}\n\n`;
            body += `🤖 **Modelos ML Utilizados:**\n`;
            body += `- Detector Binario de Vulnerabilidades (Accuracy: 79.01%)\n`;
            body += `- Clasificador de CWE (Accuracy: 86.94%)\n\n`;
            body += `📊 **Resultados del Análisis:**\n`;
            body += `- Total de archivos analizados: ${report.summary?.total || 0}\n`;
            body += `- Archivos seguros: ${report.summary?.safe || 0}\n`;
            body += `- Vulnerabilidades encontradas: ${report.summary?.vulnerable || 0}\n\n`;
            
            if (!report.is_safe && report.vulnerabilities && report.vulnerabilities.length > 0) {
              body += `### 🚨 Top 10 Vulnerabilidades Detectadas por ML:\n\n`;
              const vulns = report.vulnerabilities.slice(0, 10);
              vulns.forEach((vuln, idx) => {
                body += `**${idx + 1}. ${vuln.type || 'Unknown'}** (${(vuln.confidence * 100).toFixed(1)}%)\n`;
                body += `- 📄 \`${vuln.file}:${vuln.line}\`\n`;
                if (vuln.code) {
                  body += `- 💻 \`${vuln.code.substring(0, 40)}...\`\n`;
                }
                body += `\n`;
              });
              if (report.vulnerabilities.length > 10) {
                body += `\n... y ${report.vulnerabilities.length - 10} vulnerabilidades más\n`;
              }
              body += `\n⚠️ **Acción requerida:** Corregir las vulnerabilidades detectadas por el modelo ML antes de hacer merge.\n`;
            } else {
              body += `### ✅ Código Seguro Validado por ML\n\n`;
              body += `✨ El análisis con modelos de Machine Learning no detectó vulnerabilidades conocidas.\n\n`;
              body += `🔍 **Verificaciones realizadas:**\n`;
              body += `- Análisis binario de seguridad (Detector de Vulnerabilidades)\n`;
              body += `- Clasificación de patrones CWE (Clasificador de Weaknesses)\n`;
              body += `- Validación de ${report.summary?.total || 0} archivo(s) de código\n\n`;
              body += `✅ El código está listo para merge según los criterios de seguridad ML.\n`;
            }
            
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: body
            });
      
      - name: � Create issue for vulnerabilities
        if: steps.scan.outputs.vulnerable == 'true'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const report = JSON.parse(fs.readFileSync('vulnerability_report.json', 'utf8'));
            
            let body = `## 🔴 Vulnerabilidades Detectadas por ML\n\n`;
            body += `**Pull Request:** #${{ github.event.pull_request.number }}\n`;
            body += `**Autor:** @${{ github.event.pull_request.user.login }}\n`;
            body += `**Rama:** ${{ github.head_ref }} → ${{ github.base_ref }}\n\n`;
            body += `### 📊 Resumen\n`;
            body += `- Vulnerabilidades detectadas: ${report.vulnerabilities?.length || 0}\n`;
            body += `- Total de archivos: ${report.summary?.total || 0}\n\n`;
            body += `### 🚨 Detalles\n\n`;
            
            const vulns = report.vulnerabilities || [];
            vulns.slice(0, 10).forEach((vuln, idx) => {
              body += `${idx + 1}. **${vuln.type}** (${(vuln.confidence * 100).toFixed(1)}%)\n`;
              body += `   📄 \`${vuln.file}:${vuln.line}\`\n`;
              if (vuln.code) {
                body += `   💻 \`${vuln.code.substring(0, 50)}...\`\n`;
              }
              body += `\n`;
            });
            
            if (vulns.length > 10) {
              body += `... y ${vulns.length - 10} vulnerabilidades más\n\n`;
            }
            
            body += `### ⚠️ Acción Requerida\n`;
            body += `1. Revisar y corregir las vulnerabilidades detectadas\n`;
            body += `2. Actualizar el Pull Request con los cambios\n`;
            body += `3. El pipeline volverá a ejecutarse automáticamente\n\n`;
            body += `---\n`;
            body += `🤖 Detectado por: ML Vulnerability Scanner (Random Forest)\n`;
            
            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: `🔴 Vulnerabilidades en PR #${{ github.event.pull_request.number }}`,
              body: body,
              labels: ['security', 'vulnerability', 'automated']
            });
      
      - name: 🏷️ Add label - Scan Failed
        if: steps.scan.outputs.scan_status == 'failed'
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.issues.addLabels({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              labels: ['scan-failed', 'needs-attention']
            });
      
      - name: 🏷️ Add label - Vulnerable
        if: steps.scan.outputs.vulnerable == 'true'
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.issues.addLabels({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              labels: ['vulnerable', 'security-issue']
            });
      
      - name: ❌ Fail if vulnerable or scan failed
        if: steps.scan.outputs.vulnerable == 'true' || steps.scan.outputs.scan_status == 'failed'
        run: |
          if [ "${{ steps.scan.outputs.scan_status }}" == "failed" ]; then
            echo "❌ Escaneo de seguridad falló - Pipeline bloqueado"
            echo "No se pudo completar el análisis ML"
          else
            echo "❌ Vulnerabilidades detectadas - Pipeline fallido"
            echo "El código NO puede ser mergeado"
          fi
          exit 1

  auto-merge-to-test:
    name: 🔀 Auto Merge to Test
    needs: [security-analysis]
    runs-on: ubuntu-latest
    if: success() && github.base_ref == 'test'
    permissions:
      contents: write
      pull-requests: write
      actions: write
    
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0
      
      - name: 🔀 Auto merge PR
        uses: actions/github-script@v7
        with:
          script: |
            const pr = context.payload.pull_request;
            
            try {
              // Intentar hacer merge automático
              await github.rest.pulls.merge({
                owner: context.repo.owner,
                repo: context.repo.repo,
                pull_number: pr.number,
                commit_title: `🔀 Auto-merge: ${pr.title}`,
                commit_message: `Código aprobado por análisis ML de seguridad.\n\n✅ Sin vulnerabilidades detectadas\n✅ Listo para pruebas en rama test`,
                merge_method: 'merge'
              });
              
              console.log('✅ PR mergeado automáticamente a test');
              
              // Agregar comentario
              await github.rest.issues.createComment({
                issue_number: pr.number,
                owner: context.repo.owner,
                repo: context.repo.repo,
                body: '🤖 **Merge automático realizado**\n\n✅ Código aprobado por análisis ML de seguridad\n🚀 Ahora se ejecutarán las pruebas automáticas en la rama `test`'
              });
              
            } catch (error) {
              console.log('⚠️ No se pudo hacer merge automático:', error.message);
              core.setFailed('Error en merge automático: ' + error.message);
            }
      
      - name: 📱 Notificar merge exitoso e inicio de tests
        if: success()
        run: |
          MESSAGE="🔀 <b>MERGE AUTOMÁTICO EXITOSO</b>%0A%0A"
          MESSAGE+="📦 <b>Repositorio:</b> ${{ github.repository }}%0A"
          MESSAGE+="🔀 <b>PR:</b> #${{ github.event.pull_request.number }}%0A"
          MESSAGE+="👤 <b>Autor:</b> ${{ github.event.pull_request.user.login }}%0A"
          MESSAGE+="🌿 <b>Rama:</b> ${{ github.head_ref }} → test%0A%0A"
          MESSAGE+="✅ <b>Código aprobado por ML</b>%0A"
          MESSAGE+="🧪 <b>Siguiente etapa:</b> Iniciando pruebas automáticas...%0A%0A"
          MESSAGE+="📝 <b>Tests a ejecutar:</b>%0A"
          MESSAGE+="  • Tests del servidor (Jest)%0A"
          MESSAGE+="  • Tests del cliente (Jest)%0A%0A"
          MESSAGE+="⏳ <b>Estado:</b> Ejecutando tests...%0A%0A"
          MESSAGE+="🔗 <a href='${{ github.event.pull_request.html_url }}'>Ver Pull Request</a>"
          
          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -d "chat_id=${TELEGRAM_CHAT_ID}" \
            -d "text=${MESSAGE}" \
            -d "parse_mode=HTML"
      
      - name: 🧪 Disparar test-pipeline
        if: success()
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'test-pipeline.yml',
              ref: 'test'
            });
            console.log('✅ Test pipeline disparado');
      
      - name: 📱 Notificar error en merge
        if: failure()
        run: |
          MESSAGE="❌ <b>ERROR EN MERGE AUTOMÁTICO</b>%0A%0A"
          MESSAGE+="📦 <b>Repositorio:</b> ${{ github.repository }}%0A"
          MESSAGE+="🔀 <b>PR:</b> #${{ github.event.pull_request.number }}%0A"
          MESSAGE+="🌿 <b>Rama:</b> ${{ github.head_ref }} → test%0A%0A"
          MESSAGE+="⚠️ No se pudo completar el merge automático%0A"
          MESSAGE+="🔍 Revisar posibles conflictos%0A%0A"
          MESSAGE+="🔗 <a href='${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}'>Ver Logs</a>"
          
          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -d "chat_id=${TELEGRAM_CHAT_ID}" \
            -d "text=${MESSAGE}" \
            -d "parse_mode=HTML"
