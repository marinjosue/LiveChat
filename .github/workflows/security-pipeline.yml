name: Vulnerability Detection Pipeline

on:
  push:
    branches: [ dev ]
    paths:
      - '**.py'
      - '**.js'
      - '**.java'
      - '**.cpp'
      - '**.cs'
      - '**.php'
      - '**.rb'
      - '**.swift'
      - '**.go'
      - '**.kt'
      - '**.f90'
  pull_request:
    branches: [ dev ]

jobs:
  vulnerability-scan:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout código
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
    
    - name: 🐍 Configurar Python 3.10
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        cache: 'pip'
    
    - name: 📦 Instalar dependencias
      run: |
        python -m pip install --upgrade pip
        pip install scikit-learn pandas numpy requests
    
    - name: 📥 Descargar modelos entrenados
      run: |
        mkdir -p models
        # Copiar modelos si existen en ml-security/models
        if [ -d "ml-security/models" ]; then
          echo "📋 Copiando modelos desde ml-security/models..."
          cp ml-security/models/*.pkl models/ 2>/dev/null || echo "⚠️ Algunos modelos no se encontraron"
          ls -lah models/
        else
          echo "⚠️ Directorio ml-security/models no existe"
        fi
    
    - name: 🔍 Analizar código con modelo ML
      run: |
        python .github/scripts/scan_vulnerabilities.py
      continue-on-error: true
    
    - name: 📊 Publicar reporte de vulnerabilidades
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: vulnerability-report
        path: vulnerability_report.json
        retention-days: 30
    
    - name: 💬 Comentar en PR
      if: github.event_name == 'pull_request' && always()
      uses: actions/github-script@v6
      with:
        script: |
          const fs = require('fs');
          try {
            const report = JSON.parse(fs.readFileSync('vulnerability_report.json', 'utf8'));
            
            if (report.vulnerabilities.length > 0) {
              let comment = '## ⚠️ Vulnerabilidades Detectadas\n\n';
              report.vulnerabilities.forEach((vuln, idx) => {
                comment += `### ${idx + 1}. ${vuln.file}\n`;
                comment += `- **Tipo**: ${vuln.type}\n`;
                comment += `- **Confianza**: ${(vuln.confidence * 100).toFixed(1)}%\n`;
                comment += `- **Línea**: ${vuln.line || 'N/A'}\n\n`;
              });
              comment += `**Total vulnerabilidades detectadas**: ${report.vulnerabilities.length}`;
              
              github.rest.issues.createComment({
                issue_number: context.issue.number,
                owner: context.repo.owner,
                repo: context.repo.repo,
                body: comment
              });
            } else {
              github.rest.issues.createComment({
                issue_number: context.issue.number,
                owner: context.repo.owner,
                repo: context.repo.repo,
                body: '✅ No se detectaron vulnerabilidades en este PR'
              });
            }
          } catch (error) {
            console.log('⚠️ No se pudo procesar el reporte:', error);
          }
    
    - name: 🔍 Verificar configuración de Telegram
      run: |
        if [ -z "$TELEGRAM_BOT_TOKEN" ]; then
          echo "❌ TELEGRAM_BOT_TOKEN no configurado"
        else
          echo "✅ TELEGRAM_BOT_TOKEN configurado"
        fi
        if [ -z "$TELEGRAM_CHAT_ID" ]; then
          echo "❌ TELEGRAM_CHAT_ID no configurado"
        else
          echo "✅ TELEGRAM_CHAT_ID configurado"
        fi
      env:
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
      continue-on-error: true
    
    - name: 📱 Notificar vulnerabilidades a Telegram
      if: always()
      run: python .github/scripts/notify_telegram.py
      env:
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        GITHUB_COMMIT_MESSAGE: ${{ github.event.head_commit.message || 'Sin mensaje' }}
        GITHUB_ACTOR: ${{ github.actor }}
      continue-on-error: true
    
    - name: ❌ Fallar si hay cualquier vulnerabilidad
      run: python .github/scripts/check_vulnerabilities.py
      continue-on-error: false
