name: 🚀 Deploy to Production

on:
  pull_request:
    branches: [main]
    types: [closed]  # Se activa cuando se cierra un PR

env:
  TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
  TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}

jobs:
  deploy:
    name: 🚀 Deploy Application
    runs-on: ubuntu-latest
    if: github.event.pull_request.merged == true  # Solo si el PR fue mergeado
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
      
      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
      
      - name: 🔨 Build Cliente
        run: |
          cd cliente
          npm ci
          npm run build
      
      - name: 🔨 Build Servidor
        run: |
          cd servidor
          npm ci
      
      - name: 🐳 Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      
      - name: 🐳 Build Docker images
        run: |
          docker build -t livechat-cliente:latest -f Dockerfile.cliente .
          docker build -t livechat-servidor:latest -f Dockerfile.servidor .
      
      - name: 📱 Notify deployment start
        run: |
          MESSAGE="🚀 <b>DESPLIEGUE A PRODUCCIÓN INICIADO</b>%0A%0A"
          MESSAGE+="📦 <b>Repositorio:</b> ${{ github.repository }}%0A"
          MESSAGE+="🔀 <b>PR Mergeado:</b> #${{ github.event.pull_request.number }}%0A"
          MESSAGE+="👤 <b>Autor:</b> ${{ github.event.pull_request.user.login }}%0A"
          MESSAGE+="🌿 <b>Rama:</b> test → main%0A%0A"
          MESSAGE+="✅ <b>Validaciones completadas:</b>%0A"
          MESSAGE+="  • Seguridad ML: ✅%0A"
          MESSAGE+="  • Tests automáticos: ✅%0A%0A"
          MESSAGE+="⏳ Construyendo y desplegando aplicación..."
          
          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -d "chat_id=${TELEGRAM_CHAT_ID}" \
            -d "text=${MESSAGE}" \
            -d "parse_mode=HTML"
      
      # Aquí se integraría con el proveedor de deployment (Render, Railway, etc.)
      # Ejemplo para Railway:
      # - name: 🚂 Deploy to Railway
      #   run: |
      #     npm install -g @railway/cli
      #     railway up -s livechat-app
      
      - name: ✅ Deployment successful
        run: |
          echo "✅ Despliegue completado"
      
      - name: 📱 Notify deployment success
        if: success()
        run: |
          MESSAGE="✅ <b>DESPLIEGUE EXITOSO A PRODUCCIÓN</b>%0A%0A"
          MESSAGE+="📦 <b>Repositorio:</b> ${{ github.repository }}%0A"
          MESSAGE+="🔀 <b>PR:</b> #${{ github.event.pull_request.number }}%0A"
          MESSAGE+="👤 <b>Autor:</b> ${{ github.event.pull_request.user.login }}%0A%0A"
          MESSAGE+="🎉 <b>¡La aplicación está en producción!</b>%0A%0A"
          MESSAGE+="📊 <b>Pipeline completado:</b>%0A"
          MESSAGE+="  ✅ dev → Análisis ML%0A"
          MESSAGE+="  ✅ test → Tests automáticos%0A"
          MESSAGE+="  ✅ main → Desplegado%0A%0A"
          MESSAGE+="🌐 <b>URL de producción:</b> [Configurar URL]"
          
          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -d "chat_id=${TELEGRAM_CHAT_ID}" \
            -d "text=${MESSAGE}" \
            -d "parse_mode=HTML"
      
      - name: 📱 Notify deployment failure
        if: failure()
        run: |
          MESSAGE="❌ <b>ERROR EN DESPLIEGUE A PRODUCCIÓN</b>%0A%0A"
          MESSAGE+="📦 <b>Repositorio:</b> ${{ github.repository }}%0A"
          MESSAGE+="🔀 <b>PR:</b> #${{ github.event.pull_request.number }}%0A"
          MESSAGE+="👤 <b>Autor:</b> ${{ github.event.pull_request.user.login }}%0A%0A"
          MESSAGE+="❌ <b>Error durante el despliegue</b>%0A"
          MESSAGE+="⚠️ La aplicación NO se desplegó a producción%0A%0A"
          MESSAGE+="🔧 <b>Acción requerida:</b>%0A"
          MESSAGE+="  1. Revisar logs del workflow%0A"
          MESSAGE+="  2. Corregir el problema%0A"
          MESSAGE+="  3. Reintentar deployment%0A%0A"
          MESSAGE+="🔗 <a href='${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}'>Ver Logs Detallados</a>"
          
          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -d "chat_id=${TELEGRAM_CHAT_ID}" \
            -d "parse_mode=HTML" \
            -d "text=${MESSAGE}"
