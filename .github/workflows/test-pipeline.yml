name: 🧪 Test Pipeline - Automated Testing

on:
  push:
    branches: [test]
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write
  issues: write

env:
  TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
  TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}

jobs:
  run-tests:
    name: 🧪 Run Automated Tests
    runs-on: ubuntu-latest
    strategy:
      matrix:
        component: [servidor, cliente]
      fail-fast: false
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
      
      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: ${{ matrix.component }}/package-lock.json
      
      - name: 📦 Install dependencies - ${{ matrix.component }}
        run: |
          cd ${{ matrix.component }}
          npm ci
      
      - name: 🧪 Run tests - ${{ matrix.component }}
        id: test
        continue-on-error: true
        run: |
          cd ${{ matrix.component }}
          npm test -- --coverage --passWithNoTests
          echo "test_result=$?" >> $GITHUB_OUTPUT
      
      - name: 📊 Upload coverage
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-${{ matrix.component }}
          path: ${{ matrix.component }}/coverage/
          retention-days: 30
      
      - name: ✅ Notificar tests exitosos
        if: steps.test.outputs.test_result == '0'
        run: |
          MESSAGE="✅ <b>TESTS PASADOS</b>%0A%0A"
          MESSAGE+="📦 <b>Componente:</b> ${{ matrix.component }}%0A"
          MESSAGE+="🌿 <b>Rama:</b> test%0A"
          MESSAGE+="📊 <b>Resultado:</b> Todos los tests pasaron%0A%0A"
          MESSAGE+="✅ El código está funcionando correctamente"
          
          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -d "chat_id=${TELEGRAM_CHAT_ID}" \
            -d "text=${MESSAGE}" \
            -d "parse_mode=HTML"
      
      - name: ❌ Notificar tests fallidos
        if: steps.test.outputs.test_result != '0'
        run: |
          MESSAGE="❌ <b>TESTS FALLIDOS</b>%0A%0A"
          MESSAGE+="📦 <b>Componente:</b> ${{ matrix.component }}%0A"
          MESSAGE+="🌿 <b>Rama:</b> test%0A"
          MESSAGE+="❌ <b>Resultado:</b> Algunos tests fallaron%0A%0A"
          MESSAGE+="⚠️ <b>Acción requerida:</b> Corregir los tests antes de continuar%0A%0A"
          MESSAGE+="🔗 <a href='${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}'>Ver Logs Detallados</a>"
          
          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -d "chat_id=${TELEGRAM_CHAT_ID}" \
            -d "text=${MESSAGE}" \
            -d "parse_mode=HTML"
          
          exit 1

  create-pr-to-main:
    name: 🚀 Create PR to Main
    needs: [run-tests]
    runs-on: ubuntu-latest
    if: success()
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: 🔍 Verificar si ya existe PR
        id: check-pr
        uses: actions/github-script@v7
        with:
          script: |
            const { data: pulls } = await github.rest.pulls.list({
              owner: context.repo.owner,
              repo: context.repo.repo,
              state: 'open',
              head: `${context.repo.owner}:test`,
              base: 'main'
            });
            
            if (pulls.length > 0) {
              console.log('✅ Ya existe un PR abierto:', pulls[0].html_url);
              core.setOutput('pr_exists', 'true');
              core.setOutput('pr_number', pulls[0].number);
              core.setOutput('pr_url', pulls[0].html_url);
            } else {
              console.log('📝 No existe PR, se creará uno nuevo');
              core.setOutput('pr_exists', 'false');
            }
      
      - name: 📝 Crear PR a main
        if: steps.check-pr.outputs.pr_exists == 'false'
        id: create-pr
        uses: actions/github-script@v7
        with:
          script: |
            const { data: pr } = await github.rest.pulls.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: '🚀 Deploy to Production',
              head: 'test',
              base: 'main',
              body: `## 🚀 Despliegue a Producción
              
              Este PR ha sido creado automáticamente después de pasar todas las validaciones:
              
              ✅ **Análisis de Seguridad ML:** APROBADO
              ✅ **Tests Automáticos:** TODOS PASARON
              
              ### 📊 Validaciones Completadas
              
              1. **Seguridad (ML):**
                 - Detector de Vulnerabilidades (79% accuracy)
                 - Clasificador CWE (87% accuracy)
                 - Resultado: Sin vulnerabilidades detectadas
              
              2. **Tests Automáticos:**
                 - Tests del servidor: ✅ Pasados
                 - Tests del cliente: ✅ Pasados
                 - Cobertura de código: Disponible en artifacts
              
              ### 🔄 Flujo Completado
              
              \`\`\`
              dev → test (✅ Seguro) → main (✅ Tests Pasados) → Production
              \`\`\`
              
              ### ⚠️ Importante
              
              Este código está listo para producción. Revisar y aprobar para desplegar.
              
              ---
              
              🤖 *Creado automáticamente por el Pipeline CI/CD*
              📅 *Fecha: ${new Date().toISOString()}*
              🔗 *Workflow: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}*
              `
            });
            
            console.log('✅ PR creado:', pr.html_url);
            core.setOutput('pr_number', pr.number);
            core.setOutput('pr_url', pr.html_url);
            
            // Agregar label
            await github.rest.issues.addLabels({
              issue_number: pr.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              labels: ['ready-for-production', 'automated']
            });
      
      - name: 📱 Notificar creación de PR
        run: |
          if [ "${{ steps.check-pr.outputs.pr_exists }}" == "true" ]; then
            PR_URL="${{ steps.check-pr.outputs.pr_url }}"
            PR_NUM="${{ steps.check-pr.outputs.pr_number }}"
          else
            PR_URL="${{ steps.create-pr.outputs.pr_url }}"
            PR_NUM="${{ steps.create-pr.outputs.pr_number }}"
          fi
          
          MESSAGE="🚀 <b>LISTO PARA PRODUCCIÓN</b>%0A%0A"
          MESSAGE+="📦 <b>Repositorio:</b> ${{ github.repository }}%0A"
          MESSAGE+="🔀 <b>PR:</b> #${PR_NUM}%0A"
          MESSAGE+="🌿 <b>Rama:</b> test → main%0A%0A"
          MESSAGE+="✅ <b>Análisis ML:</b> APROBADO%0A"
          MESSAGE+="✅ <b>Tests Automáticos:</b> TODOS PASADOS%0A%0A"
          MESSAGE+="🚀 <b>El código está listo para producción</b>%0A%0A"
          MESSAGE+="🔗 <a href='${PR_URL}'>Revisar y Aprobar PR</a>"
          
          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -d "chat_id=${TELEGRAM_CHAT_ID}" \
            -d "text=${MESSAGE}" \
            -d "parse_mode=HTML"
  
  notify-failure:
    name: 📱 Notify Test Failure
    needs: [run-tests]
    runs-on: ubuntu-latest
    if: failure()
    
    steps:
      - name: 📱 Notificar fallo en tests
        run: |
          MESSAGE="❌ <b>PIPELINE BLOQUEADO</b>%0A%0A"
          MESSAGE+="📦 <b>Repositorio:</b> ${{ github.repository }}%0A"
          MESSAGE+="🌿 <b>Rama:</b> test%0A"
          MESSAGE+="❌ <b>Estado:</b> Tests fallidos%0A%0A"
          MESSAGE+="⚠️ <b>El código NO puede ir a producción</b>%0A"
          MESSAGE+="🔧 Debes corregir los tests que fallaron%0A%0A"
          MESSAGE+="📝 <b>Acciones requeridas:</b>%0A"
          MESSAGE+="1. Revisar los logs de tests%0A"
          MESSAGE+="2. Corregir los problemas%0A"
          MESSAGE+="3. Push a dev → PR a test nuevamente%0A%0A"
          MESSAGE+="🔗 <a href='${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}'>Ver Logs Detallados</a>"
          
          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -d "chat_id=${TELEGRAM_CHAT_ID}" \
            -d "text=${MESSAGE}" \
            -d "parse_mode=HTML"
